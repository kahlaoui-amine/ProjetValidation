<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WeatherApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/gradient_primary</item>
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/rimouski</item>
        <item name="android:textColor">@color/text_color_primary</item>
        <item name="android:colorEdgeEffect">@color/gradient_primary</item>
    </style>

    <!-- Settings theme. -->
    <style name="Theme.WeatherApp.Settings" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gradient_primary</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/gradient_primary</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/gradient_primary</item>
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/rimouski</item>
        <item name="android:textColor">@color/background</item>
        <item name="android:colorEdgeEffect">@color/gradient_primary</item>
        <item name="titleTextColor">@color/text_color_primary</item>
    </style>
</resources>